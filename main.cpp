#include <iostream>
#include "all.h"

int main()
{

    // double height = 1.75;

    // double mass = 75.0;

    // double bmi = mass / (height*height);

    // auto status = [](double bmi) {
    //     if (bmi < 18.5)
    //         return "Underweight\n"s;
    //     else if (bmi < 25.0)
    //         return "Normal\n"s;
    //     else if (bmi < 30.0)
    //         return "Overweight\n"s;
    //     else 
    //         return "Obese\n"s;
    // };

    // std::cout << status(bmi);
}
